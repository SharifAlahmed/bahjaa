# خطة إعادة تصميم بهجة — مرجع Claude Code

> هذا الملف هو المرجع الوحيد لإعادة التصميم. نفّذ **مرحلة واحدة فقط** في كل جلسة، ثم توقّف وأعطني تقريراً. لا تنتقل للمرحلة التالية دون إذني.
> المعاينات المرجعية في `docs/design-previews/`. افتحها لترى الشكل المطلوب قبل كتابة أي كود.

---

## القواعد الثابتة (تسري على كل المراحل)

1. **ملف رموز واحد.** لا قيمة hex خارجه. إن احتجت لوناً ليس فيه، توقّف واسألني.
2. **RTL بخصائص منطقية فقط.** ممنوع: `margin/padding/border-left/right`، `left/right`، `text-align:left/right`، وفي Tailwind: `ml- mr- pl- pr- left- right- border-l- border-r-`. البديل: `ms- me- ps- pe- start- end- border-s- border-e-` و`inset-inline-*` و`text-align:start/end`.
3. **الأخضر والنحاسي على الأسطح الفاتحة بنسختهما الحبرية:** `#177F5E` و`#8E6A29`. الأصليان `#1D9E75` و`#C99A45` داخل الأسطح الداكنة فقط.
4. **الحد الأدنى للتباين:** ٤٫٥:١ للنص، و٣:١ للأيقونات والحدود. الألوان الشفافة تُحسب بعد دمجها مع خلفيتها. أقل شفافية مقبولة للنص الثانوي على الورق: `.67`.
5. **كل رقم يراه القارئ بالأرقام العربية الهندية** (٠١٢٣٤٥٦٧٨٩). والجمع عبر دالة: دقيقة واحدة / دقيقتان / ٣–١٠ دقائق / ١١+ دقيقة.
6. **لا رقم لا نملكه، ولا نص مخترع.** أي عدد ظاهر إمّا ثابت في النموذج (١٠ أقسام، ٤ مفتوحة) أو من البيانات. لا تكتب مقتطفاً أو وصفاً لكتاب من عندك.
7. **الشعار لا يُمس:** الملف الأصلي من `public/` كما هو، بلا إعادة رسم أو تلوين أو توليد بالكود.
8. **لوحة داكنة واحدة في كل صفحة** (عدا الترويسة والتذييل)، و**زر مصمت واحد في كل منطقة**؛ الثاني شبحي أو نصي.
9. **لا `letter-spacing` على أي نص عربي.**
10. **الصفحات التي تقرأ الجلسة تبقى ديناميكية (ƒ)** ولا يتسلّل إليها `revalidate`.
11. **لا تنشئ صفحة أو رابطاً لصفحة غير موجودة.** اقرأ `app/` أولاً.
12. **كل مرحلة على فرع `redesign`**، لا على `main`. الدفع إلى الفرع يعطي رابط معاينة من Vercel.
13. **لا تشغّل `next build` ولا `next dev` محلياً** — الجهاز بطيء. الفحص الوحيد: `npx tsc --noEmit` ثم grep، ثم ادفع إلى Vercel.
14. **أسماء الملفات بأحرف صغيرة.** لا تحرّر `package.json` نصّياً — استخدم `npm pkg delete` / `npm pkg set`. وقبل كل دفع شغّل هذه الثلاثة (ولا تشغّل `npm run build` محلياً، فالجهاز بطيء):
    `node -e "require('./package.json')"` — تحقّق أن JSON صالح
    `git ls-files <أي مجلد لمسته>` — تأكّد أن git يطابق القرص
    `npx tsc --noEmit` — لا أخطاء TypeScript

### فحص نهاية كل مرحلة
```bash
# ١. hex خارج ملف الرموز
grep -rnE "#[0-9a-fA-F]{3,6}\b" app components --include=*.tsx --include=*.ts --include=*.css | grep -v "<ملف الرموز>"
# ٢. خصائص فيزيائية
grep -rnE "\b(ml|mr|pl|pr|left|right|border-l|border-r)-[0-9a-z\[]|text-(left|right)|(margin|padding|border)-(left|right)" app components
# ٣. البناء
next build
```
ثم افتح رابط المعاينة وتحقّق يدوياً عند عرض ٣٦٠ و٣٩٠ بكسل:
`document.documentElement.scrollWidth - document.documentElement.clientWidth` يجب أن يساوي `0`.

---

## المرحلة ٠ — الرموز والترويسة والتذييل

### ٠-أ · ملف الرموز
قبل أي تغيير: حدّد أين تعيش الرموز اليوم (`app/globals.css` أو ما يعادله)، وأعطني قائمة بكل قيمة hex في المشروع. لا تنشئ ملفاً ثانياً.

ضع هذه الرموز في `@theme` (إن كان Tailwind v4) أو `:root`:

```css
--color-brand-primary:#1D9E75;
--color-brand-dark:#15332A;
--color-brand-mint:#3ED69F;
--color-deep:#0B1412;
--color-background:#F7F5EE;
--color-surface:#FFFFFF;
--color-border:#E6ECE9;
--color-accent-gold:#C99A45;
--color-brand-ink:#177F5E;
--color-accent-ink:#8E6A29;
--color-ink-muted:rgba(21,51,42,.67);

--radius-btn:6px;
--radius-card:10px;
--radius-panel:14px;

--shadow-soft:0 1px 2px rgba(11,20,18,.04),0 8px 24px rgba(11,20,18,.06);
--shadow-book:0 2px 4px rgba(11,20,18,.18),0 18px 32px -8px rgba(11,20,18,.28);
--shadow-book-hover:0 3px 6px rgba(11,20,18,.2),0 26px 40px -10px rgba(11,20,18,.34);

--space-section:clamp(56px,8vw,96px);
```

- الخطوط عبر `next/font/google`: **Tajawal** (400/500/700/800) للعربية، و**Inter** للاتيني. تأكّد أن `button` و`input` يرثان الخط (`font: inherit`).
- أصناف الأزرار:
  - `.btn-primary`: خلفية brand-dark، نص surface.
  - `.btn-ghost`: حد ١٫٥ بكسل brand-dark، نص brand-dark، خلفية شفافة.
  - `.btn-accent` (داخل اللوحة الداكنة فقط): خلفية brand-primary، نص deep. **لا نص أبيض عليه** (٣٫٢:١ فقط).
- استبدل كل hex متفرّق بالرمز المقابل.

### ٠-ب · شريط الإعلان والترويسة
مكوّنان في `components/layout/`:

**`AnnouncementBar`:** خلفية brand-dark، نص background ١٣ بكسل وزن ٥٠٠، سطر واحد:
«الأقسام ١–٤ من كل ملخص مفتوحة للجميع، والباقي بإيميلك فقط بلا كلمة مرور».

**`SiteHeader`** (من اليمين إلى اليسار):
- **البداية:** الشعار الرسمي عبر `next/image` بارتفاع ٤٠–٤٤ بكسل.
- **الوسط:** روابط الصفحات الموجودة فعلاً فقط. Tajawal ٥٠٠ بحجم ١٥ بكسل. الرابط النشط بخط سفلي ٢ بكسل brand-ink عبر `usePathname`.
- **النهاية:** أيقونة `Search` من `lucide-react` فقط إن كان في المشروع بحث. أيقونة `User` مع «دخول» إلى مسار الدخول الحالي، وتصير «حسابي» عند وجود جلسة. الأيقونات `strokeWidth={1.5}` بحجم ٢٢.
- ثابتة أعلى الصفحة، خلفية surface، حد سفلي border، وظل soft بعد تمرير ٨ بكسل.
- رابط «تخطَّ إلى المحتوى» يظهر عند التركيز، و`:focus-visible` واضح.
- **الجوال (أقل من ٧٦٨):** زر قائمة يفتح لوحة تنزلق من جهة البداية، وتُغلق بـEsc وبالنقر خارجها.

### ٠-ج · التذييل
`components/layout/SiteFooter.tsx` على خلفية background (الورق، لا الداكن):
- ٤ أعمدة على الشاشة الكبيرة، وعمودان على الجوال:
  1. الشعار + «نحوّل المعرفة إلى أثر» (accent-ink، ٧٠٠) + «منصة عربية تحوّل الكتب إلى خطوات عملية للقادة ورواد الأعمال».
  2. «استكشف»: روابط موجودة فقط.
  3. «بهجة»: روابط موجودة فقط، وإلا احذف البند.
  4. «تواصل»: البريد عبر `mailto:` من ملف إعدادات واحد.
- عناوين الأعمدة ١٥/٨٠٠، والروابط ١٤/٤٠٠ ink-muted، وتصير brand-ink عند المرور.
- سطر أخير بحد علوي: «© ٢٠٢٦ بهجة» (السنة من دالة تحويل للأرقام العربية). الأيقونات الاجتماعية فقط للحسابات التي أعطيك روابطها؛ **اسألني عنها**.

ركّب الثلاثة في `app/layout.tsx` واحذف القديم. شغّل الفحص، ثم ادفع إلى `redesign`.

---

## المرحلة ١ — الأغلفة والبطاقات

**المرجع:** `docs/design-previews/book-cards-preview.html`

### ١-أ · قاعدة البيانات والرفع
1. افحص جدول الملخصات (`bh_` في مشروع momkin-academy) وأعطني أعمدته. إن لم يوجد عمود غلاف، أضف migration:
   `cover_url text`، و`is_featured boolean not null default false`.
2. bucket اسمه `bh-covers`: قراءة عامة، والكتابة والحذف للأدمن فقط بنفس آلية الأدمن الموجودة. حدّ الحجم ٢ ميغابايت يُفرض في إعدادات الـbucket، لا في الواجهة فقط.
3. في لوحة الأدمن: حقل رفع غلاف لكل ملخص (jpg/png/webp) يحدّث `cover_url`، وزر «اجعله مميّزاً» يضبط `is_featured` لملخص واحد فقط (دالة SQL أو trigger تلغي التمييز عن غيره).
4. أضف نطاق Supabase إلى `images.remotePatterns` في `next.config`.

### ١-ب · مكوّن `components/BookCover.tsx`
الغلاف يُخزَّن **مسطّحاً** (الواجهة الأمامية فقط، نسبة ٢:٣)، والإحساس المجسّم يُصنع بالـCSS.

```
props: { title, author, coverUrl?, size: 'card' | 'hero' | 'lg', priority? }
```
> ⚠️ لا تسمِّ الحجم `page`: يتعارض مع أي صنف عام بنفس الاسم ويخفي الغلاف.

```
.book-stage   ← الأرضية وظل الأرض
  .book       ← position:relative; aspect-ratio:2/3
    next/image   (fill، object-fit:cover، alt="غلاف كتاب {title}")
    span.book-pages (aria-hidden)
```

- **الكتاب العربي كعبه على اليمين:** المفصل عند `inset-inline-start`، والصفحات عند `inset-inline-end`.
- **الزوايا:** ٢ بكسل على جهة الكعب (`border-start-start-radius` و`border-end-start-radius`)، و٤ بكسل على الحافة الحرة.
- **المفصل (`.book::before`):** عرض ٦٪ عند `inset-inline-start`، وهذا التدرّج داخل `:dir(rtl)` (ومعكوساً بـ`to right` داخل `:dir(ltr)`):
  `linear-gradient(to left, rgba(11,20,18,.28) 0%, rgba(255,255,255,.12) 38%, transparent 60%, rgba(11,20,18,.22) 66%, transparent 76%)`
- **اللمعة (`.book::after`):** `linear-gradient(135deg, rgba(255,255,255,.06) 0%, transparent 45%, rgba(255,255,255,.06) 100%)`، مع `pointer-events:none`.
- **الصفحات (`.book-pages`):** `inset-block:4px; inset-inline-end:-6px; inline-size:6px`، وخلفية:
  `repeating-linear-gradient(to bottom, var(--color-background) 0 1px, var(--color-border) 1px 1.5px)`
- **ظل الكتاب:** `var(--shadow-book)`.
- **ظل الأرض (`.book-stage::after`):** بيضاوي، عرض ٨٠٪، ارتفاع ١٤ بكسل، على مسافة ١٠ بكسل تحت الكتاب:
  `radial-gradient(closest-side, rgba(11,20,18,.18), transparent)`
- **الأحجام:**

  | الحجم | سطح المكتب | الجوال |
  |---|---|---|
  | `card` | ٧٢٪ من منطقة الغلاف | ٨٠٪ |
  | `hero` | ٢٤٠ بكسل | ١٦٠ بكسل |
  | `lg` | ٢٢٠ بكسل | ١٠٤ بكسل |

- **التحميل:** `sizes` مناسبة، ولون border مكانه حتى تصل الصورة. `priority` لغلاف الهيرو فقط.
- **الاحتياطي عند غياب `coverUrl`:** غلاف طباعي داخل نفس `.book` بالمفصل والصفحات والظل نفسها: خلفية brand-dark، العنوان Tajawal ٨٠٠، خط ذهبي قصير، والمؤلف.

### ١-ج · البطاقة `SummaryCard`
- **منطقة الغلاف:** `aspect-ratio:4/5`، حشو `24px 20px 26px` (١٦ بكسل على الجوال)، وخلفية:
  `radial-gradient(120% 70% at 50% 100%, var(--color-background) 0%, var(--color-surface) 70%)`
- **تحتها** بحد علوي border: التصنيف (١٣/٧٠٠ brand-ink)، ثم العنوان (١٥/٧٠٠)، ثم «اقرأ الملخص» (١٤/٧٠٠ brand-ink). **لا سطر للمؤلف**، فهو على الغلاف.
- **البطاقة كلها رابط واحد.** عند المرور: الكتاب يرتفع ٤ بكسل وظله يصير `--shadow-book-hover` (٢٠٠ms)، ومعطّل مع `prefers-reduced-motion`. لا دوران.
- **الشبكة:** ٤ أعمدة، و٣ عند ٩٨٠ بكسل، وعمودان على الجوال (لا عمود واحد أبداً).

استخدم `BookCover` و`SummaryCard` في كل مكان تُعرض فيه الملخصات اليوم. فحص، ثم دفع.

---

## المرحلة ٢ — الرئيسية

**المرجع:** `docs/design-previews/site-prototype.html` (صفحة الرئيسية).

### ٢-أ · الهيرو `components/home/Hero.tsx`
- عمودان: النص في البداية (يمين)، والمسرح في النهاية.
- سطر علوي «نحوّل المعرفة إلى أثر» (brand-ink ١٤/٧٠٠).
- العنوان «المعرفة التي تستحق أن تُطبَّق»: Tajawal ٨٠٠، `clamp(30px,4.6vw,52px)`، ارتفاع سطر ١٫٢٨.
- الفقرة (١٨ بكسل ink-muted، عرض أقصى ٤٨٠): «ملخّصات عربية لأهمّ الكتب، مبنية للقائد ورائد الأعمال: تفهم الفكرة، ثم تعرف ماذا تفعل بها».
- زرّان: `.btn-primary` «ابدأ القراءة» إلى الملخص المميّز، و`.btn-ghost` «تصفّح المكتبة».
- صف ضمانات بأيقونات lucide (`ShieldCheck`, `Sprout`, `PenLine`) بلون brand-ink: «مصادر موثوقة»، «خطوة تطبيق في كل ملخص»، «بالعربية، للقائد المشغول». بلا أعداد.
- **المسرح:** بطاقة surface بزوايا panel وظل soft، وفي وسطها `BookCover size="hero"` للملخص المميّز (`is_featured`، وإلا الأحدث نشراً). الغلاف رابط لصفحته.
- **الجوال:** الغلاف يصغر ويقف بجانب العنوان لا فوقه، ويُخفى صف الضمانات.

### ٢-ب · أحدث الملخصات
- رأس القسم: «أحدث الملخصات» (٣٠/٨٠٠)، وتحته «ما نُشر مؤخراً في المكتبة.»، ورابط نصي «عرض المكتبة كاملة» في النهاية.
- آخر ٨ ملخصات منشورة، مرتبة بـ`published_at desc`، في `SummaryCard`. لا «الأكثر قراءة» ولا أي ادّعاء شعبية.

### ٢-ج · شريط المسار `PathStrip`
- عرض كامل، خلفية surface (**لا الرمادي**: يُسقط النص إلى ٤٫١٤:١)، وحدّان علوي وسفلي border.
- ٤ خانات بفواصل `border-inline-start` (عدا الأولى). على الجوال شبكة ٢×٢.
- أيقونات lucide بحجم ٢٨ و`strokeWidth` ١٫٥ بلون brand-ink، العنوان ١٥/٧٠٠، والنص ١٤/٤٠٠ ink-muted:

  | الأيقونة | العنوان | النص |
  |---|---|---|
  | `BookOpen` | المعرفة | من كتاب يستحق وقتك. |
  | `Lightbulb` | الفهم | الفكرة كما تمسّ عملك أنت. |
  | `ListChecks` | التطبيق | خطوة واضحة تبدأ بها. |
  | `Sprout` | الأثر | ما يتغيّر فعلاً بعدها. |

### ٢-د · لوحة النشرة `NewsletterPanel`
- **اللوحة الداكنة الوحيدة في الصفحة:** احذف أي لوحة داكنة أخرى من الرئيسية.
- خلفية brand-dark، عمودان:
  - اليمين: سطر «النشرة البريدية» (accent-gold)، ثم «فكرة تستحق التطبيق، في بريدك» (٢٤/٨٠٠ بلون ورقي)، ثم «كن أول من يعرف بالملخصات الجديدة.» (ورقي بشفافية .78).
  - اليسار: نموذج beehiiv المضمّن (سأعطيك كوده)، وزر `.btn-accent`.
- **مشتركو النشرة منفصلون عن مستخدمي المنصة:** لا تربطه بجداول `bh_` ولا بالمصادقة.
- على الجوال عمود واحد.

فحص، ثم دفع.

---

## المرحلة ٣ — أريد قراءته وأعجبني ومكتبتي

**المرجع:** `docs/design-previews/reader-actions-preview.html`

### ٣-أ · قاعدة البيانات
```sql
create table bh_reactions (
  user_id uuid not null references auth.users(id) on delete cascade,
  summary_id <نوع المفتاح في جدول الملخصات> not null references <جدول الملخصات>(id) on delete cascade,
  kind text not null check (kind in ('want','like')),
  created_at timestamptz not null default now(),
  primary key (user_id, summary_id, kind)
);
create index on bh_reactions (summary_id, kind);
```
- RLS مفعّلة. سياسات `select` و`insert` و`delete` حيث `auth.uid() = user_id`. لا `update`.
- دالة `bh_reaction_counts()` للأدمن فقط (`security definer` + فحص الأدمن بنفس الآلية الموجودة): تُرجع لكل ملخص عدد want وعدد like.

### ٣-ب · رأس صفحة الملخص
- شبكة بمناطق:
  - سطح المكتب: `'cover text' 'cover reader'` (الغلاف يمتد على صفّين، و`BookCover size="lg"`).
  - الجوال: `'cover text' 'reader reader'`.
- منطقة `text`: التصنيف، العنوان، المؤلف.
- منطقة `reader`: المكوّن `components/summary/ReaderActions.tsx`.

### ٣-ج · `ReaderActions`
- **السطر الأول:** رابط «ابدأ القراءة» (Tajawal ٨٠٠، ١٨ بكسل، brand-dark) إلى `#sec-1`، وبعده `ChevronLeft` بلون **accent-ink** `#8E6A29` (لا الذهبي الفاتح)، ويتحرّك ٣ بكسل عند المرور.
- **السطر الثاني:** زرّان متجاوران:
  - «أريد قراءته» مع `Bookmark`.
  - «أعجبني» مع `Heart`.
  - التنسيق: خلفية background، حد ١ بكسل border، زوايا ١٠ بكسل، حشو `10px 16px`، Tajawal ٥٠٠ بحجم ١٥. الأيقونات ٢٠ بكسل، `strokeWidth` ١٫٥، بلون ink-muted.
  - **المفعّل:** الأيقونة ممتلئة (`fill="currentColor"`) بلون brand-ink، والحد brand-ink، والخلفية surface. **لا أحمر للقلب.**
  - `aria-pressed` يعكس الحالة، والمرور يجعل الحد brand-ink.
- **سطر حالة** تحت الزرّين (`aria-live="polite"`، ١٣ بكسل ink-muted): «حُفظ في مكتبتي.» / «أُزيل من مكتبتك.» / «سجّلنا إعجابك.» / «أُلغي الإعجاب.»
- **لا يظهر أي عدد إعجابات للقارئ.**

### ٣-د · السلوك
- **مسجّل:** toggle عبر Server Action، مع تحديث متفائل (`useOptimistic`) يعود لحالته مع رسالة إن فشل.
- **زائر:** الضغط يفتح الدخول بنيّة محفوظة في الرابط:
  `/login?next=/summary/<slug>&intent=want` (أو `like`)
  - في أعلى بطاقة الدخول صندوق فيه الغلاف مصغّراً وجملة حسب النيّة:
    - want: «أدخل إيميلك لنحفظ «<العنوان>» في مكتبتك.»
    - like: «أدخل إيميلك ليُحفظ إعجابك بـ«<العنوان>».»
  - بعد التحقّق من الرمز: نفّذ النيّة على الخادم، ثم ارجع إلى `next` والزر مفعّل، وسطر الحالة: «دخلت، وحُفظ الكتاب في مكتبتي.»
  - تحقّق أن `next` مسار داخلي يبدأ بـ`/` (لا open redirect)، وأن `intent` إحدى القيمتين فقط.
- الحالة الأولية تُقرأ على الخادم مع الصفحة. الصفحة تبقى ƒ.

### ٣-هـ · صفحة «مكتبتي»
- محمية: غير المسجّل يُحوّل إلى الدخول.
- تبويبان: «أريد قراءته» و«أعجبني»، بالأحدث أولاً، في `SummaryCard`.
- حالة الفراغ:
  - want: «لم تحفظ كتاباً بعد» / «احفظ ما يلفتك من المكتبة لتعود إليه.» + زر شبحي «تصفّح المكتبة».
  - like: «لم يُعجبك كتاب بعد» / «اضغط «أعجبني» على ملخص قرأته ليظهر هنا.»
- «حسابي» في الترويسة يقود إليها.

### ٣-و · لوحة الأدمن
عمودان في قائمة الملخصات: «يريدون قراءته» و«أعجبهم»، من `bh_reaction_counts()`، قابلان للترتيب، بالأرقام العربية.

### ٣-ز · اختبار قبل الدفع
- حسابان مختلفان: كل واحد لا يرى إلا تفاعلاته.
- مسار الزائر كاملاً: ضغط، ثم دخول، ثم رمز، ثم عودة والزر مفعّل.
- الزرّان يصلهما Tab، ويعملان بـEnter والمسافة، والتركيز ظاهر.

---

## المرحلة ٤ — الدمج

بعد مراجعتي لرابط المعاينة ومرور كل الفحوص: ادمج `redesign` في `main` وادفع.
