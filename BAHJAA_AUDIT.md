# BAHJAA_AUDIT.md
> تدقيق ما قبل تطبيق نظام الهوية · 16 سبتمبر 2026 · **قراءة فقط — لم يُعدَّل أي مكوّن**
> الحالة الحيّة: `https://bahjaa.com` · آخر التزام `6b53d8c`

---

## A. المسارات ومن يعرضها

| المسار | الملف | العرض | ملاحظة |
|---|---|---|---|
| `/` | `app/page.tsx` | `force-dynamic` | يقرأ `bh_summaries` (8) + `bh_categories` (6) |
| `/categories` | `app/categories/page.tsx` | `force-dynamic` | عدّ منشور لكل قسم |
| `/c/[slug]` | `app/c/[slug]/page.tsx` | `force-dynamic` | + `generateMetadata` |
| `/s/[slug]` | `app/s/[slug]/page.tsx` | `force-dynamic` | + `generateMetadata` · **يحمل الجدار** |
| `/login` | `app/login/page.tsx` + `login-form.tsx` (client) | — | OTP من ٨ أرقام |
| `/admin` | `app/admin/page.tsx` + `actions.ts` | — | محمي بـ `bh_is_admin()` |
| `/admin/preview/[slug]` | `app/admin/preview/[slug]/page.tsx` | — | معاينة المسودات |
| `/auth/callback` | `app/auth/callback/route.ts` | Route Handler | — |
| `/auth/signout` | `app/auth/signout/route.ts` | Route Handler | `Cache-Control: no-store` |
| `/robots.txt` | `app/robots.ts` | — | يمنع `/admin` `/login` `/auth` |
| `/sitemap.xml` | `app/sitemap.ts` | `revalidate = 3600` | ⚠️ يستدعي `createClient()` (كوكيز) |
| 404 | `app/not-found.tsx` | — | — |
| كل المسارات | `middleware.ts` | — | `supabase.auth.getUser()` لتحديث الجلسة |

**الطبقة الحرجة:** `middleware.ts` + `lib/supabase/server.ts` + `force-dynamic` على الصفحات الأربع. أي مساس بها يكسر الجلسة أو يسرّب محتوى مدفوعاً.

---

## B. أين تُعرَّف الألوان والخطوط والمسافات

### نظاما رموز متوازيان في `app/globals.css` (520 سطراً · **41 قيمة هكس**)

**١ — كتلة `@theme`** (رموز Tailwind v4):
`--color-paper #FBF7EF` · `--color-paper-2 #F3EEE3` · `--color-ink #0B1F19` · `--color-ink-soft #4A5A53` · `--color-muted #8A7A63` · `--color-deep #063B31` · `--color-deep-2 #04241D` · `--color-green #1D9E75` · `--color-brass #B07D3A` · `--color-rule #DFD5C2` · `--radius-bh 2px`

**٢ — كتلة `:root`** (CSS خام): تكرّر القيم العشر نفسها بأسماء بلا بادئة (`--paper`، `--ink`، `--deep` …) + ستة ألوان أقسام + ثلاثة متغيّرات خطوط.

> **ازدواج:** نفس القيمة معرّفة مرتين بأسماء مختلفة. المكوّنات الجديدة تستخدم `:root`، وأصناف Tailwind تستخدم `@theme`.

### طبقة توافق تُحذف
`--color-bh-primary` · `-primary-light` · `-primary-dark` · `-bg` · `-surface` · `-border` · `-text` · `-muted` — ثمانية رموز من الهوية القديمة، معاد ربطها يدوياً باللوحة الجديدة. وثلاثة أخرى لم تُربط: `--color-bh-trap-bg #FFF8E7` · `--color-bh-trap-border #EF9F27` · `--color-bh-trap-text #633806`.
**هذه أحد عشر رمزاً من نظام سابق — للحذف.**

### هكس صريح خارج ملف الرموز
| الملف | السطر | القيمة |
|---|---|---|
| `components/bahjaa/cover.tsx` | 5-10 | ألوان الأقسام الستة (`#085041` `#A8432B` `#1D9E75` `#1F3A5F` `#6B4A7E` `#B07D3A`) |
| `app/login/login-form.tsx` | 162, 230 | `#b23b2e` (لون خطأ) |
| `app/admin/page.tsx` | 141 | `#b23b2e` |

### الخطوط
مستضافة ذاتياً في `public/fonts/`: **Amiri** 400/700 · **IBM Plex Sans Arabic** 300/400/500/600 — عبر ستة `@font-face` في `globals.css`.
`app/fonts.ts` **ملف ميت** (`export {}`) — كان يستخدم `next/font/google` وعُطّل لأن شبكة جهاز التطوير محجوبة بـ proxy.

### المسافات
لا مقياس مركزي. القيم مكتوبة inline أو عبر `clamp()` داخل `globals.css`. لا يوجد سُلَّم 4px.

---

## C. المكوّنات

### النظام الجديد — `components/bahjaa/` (16 ملفاً)
| مُستخدم فعلاً | الملف |
|---|---|
| ✅ | `cover.tsx` → `/` و `/s/[slug]` · `category-row.tsx` → `/categories` · `dark-panel.tsx` · `trust-bar.tsx` · `inside-summary.tsx` · `summary-toc.tsx` · `format.ts` · `icons.tsx` |
| ❌ **ميت** | `book-card.tsx` · `hero.tsx` · `email-gate.tsx` · `today-action.tsx` · `related-books.tsx` · `summary-section.tsx` · `site-header.tsx` · `site-footer.tsx` |

**ثمانية مكوّنات غير مستوردة من أي مكان.** `site-header/footer` منها مكرّرة باسم مطابق للمستخدَمة فعلاً في `components/` — مصدر التباس مؤكد.

### النظام القديم — `components/` (7 ملفات، كلها حيّة)
`site-header.tsx` · `site-footer.tsx` (مربوطان في `layout.tsx`) · `paywall.tsx` · `section.tsx` · `summary-card.tsx` · `summary-free.tsx` · `summary-full.tsx` (222 سطراً — أكبر ملف).

### ⚠️ الهوية مطبّقة جزئياً — أهم اكتشاف في التدقيق
| السطح | الغلاف المعروض |
|---|---|
| `/` و `/s/[slug]` | `Cover` التوليدي الجديد |
| **`/c/[slug]`** | `SummaryCard` القديم → مربّع `w-16 h-24` فيه **حرف «ب»** |

صفحات الأقسام الستة ما زالت على التصميم القديم. من يدخل قسماً يرى موقعاً مختلفاً عن الرئيسية.

---

## D. عيوب RTL

| # | الموقع | العيب |
|---|---|---|
| 1 | `components/summary-free.tsx:42` | `border-r-[3px] border-r-bh-primary` — حدّ فيزيائي يمين. في RTL جهة البداية، فالصحيح `border-s`. **العيب الفيزيائي الوحيد في المستودع.** |
| 2 | `components/summary-free.tsx:76` | `text-center` على سطر عربي خارج لوحة داكنة — مخالف لقانون «اليمين هو المرساة» |
| 3 | `components/summary-full.tsx:217` | `text-center` على سطر الختام |
| 4 | `components/section.tsx:16` | `القسم {num}` — **أرقام لاتينية** تصل القارئ |
| 5 | `components/summary-card.tsx:32` | `قراءة {s.reading_minutes} دقائق` — أرقام لاتينية + صيغة جمع خاطئة عند ١ و٢ و١١+ |
| 6 | `components/summary-full.tsx:163` | «مؤشر النجاح بعد 30 يوماً» — رقم لاتيني في نص ثابت |

بقية المستودع نظيف: لا `margin-left/right`، ولا `padding-left/right`، ولا `text-align: left/right` في أي ملف.

---

## E. عيوب الجوال عند 390 بكسل

| # | الموقع | العيب |
|---|---|---|
| 1 | `.book-meta` (`globals.css`) | يصير شبكة عمودين تحت 600px — «رواد الأعمال وبناة المنتجات» في عمود ~160px |
| 2 | `components/section.tsx` | `flex` يضمّ شارة «القسم N» + عنوان 24-32px في صف واحد بلا `flex-wrap` |
| 3 | `components/summary-full.tsx:119-165` | مسار التحويل شبكة 2×2 داخل إطار — أعمدة ضيقة جداً |
| 4 | `components/summary-card.tsx` | غلاف ثابت `w-16 h-24` + `line-clamp-2` — يصمد لكنه مكتظ |
| 5 | `components/summary-free.tsx:14` | شبكة `sm:grid-cols-2` من `LabelCard` — بطاقات متلاصقة بفواصل 12px |

**لم تُقَس هذه على الموقع الحيّ بمحتوى حقيقي** — قياسي السابق كان على صفحة المعاينة لا على الإنتاج. أول بند في المرحلة 9.

---

## F. سطح SEO — ممنوع تغييره

### العناوين والأوصاف
| المسار | `title` | `description` |
|---|---|---|
| الجذر (قالب) | `%s · بهجة` · الافتراضي `بهجة — من المعرفة إلى الأثر` | `ليست ملخصات — محرك يحوّل المعرفة إلى خطوة تطبّقها اليوم. للقادة ورواد الأعمال العرب.` |
| `/s/[slug]` | `ملخص كتاب {book_title_ar}` | `content_free.s1.core_idea` أو `s4.text`، مقطوعة عند 160 حرفاً |
| `/c/[slug]` | `{name_ar}` | `description_ar` أو `ملخصات بهجة في {name_ar}` |
| `/categories` | `الأقسام` | نص ثابت يعدّد الأقسام الستة |
| `/login` | `الدخول` | `ادخل بإيميلك — بلا كلمة مرور.` |
| `/admin` | `لوحة التحكم` | — (محجوب في robots) |

### OpenGraph
الجذر: `type=website` · `locale=ar_AR` · `siteName=بهجة`.
`/s/[slug]`: عنوان + وصف + `images: [cover_url]` — **`cover_url` فارغ `null` لكل الملخصات الثمانية**، فلا صورة مشاركة اليوم.

### Canonical
**لا يوجد `alternates.canonical` في أي ملف.** Next لا يصدر وسم canonical تلقائياً. أي إضافة = تغيير في سطح SEO، خارج نطاق التطبيق البصري ما لم تُعتمد صراحةً.

### Sitemap و robots
`sitemap.xml` يبني من `NEXT_PUBLIC_SITE_URL`: الجذر + `/categories` + 6 أقسام + 8 ملخصات.
`robots.txt` الحيّ صحيح ويشير إلى `https://bahjaa.com/sitemap.xml` — أي أن متغيّر الإنتاج في Vercel مضبوط.

> ⚠️ **`.env.local` على جهاز التطوير يحمل `NEXT_PUBLIC_SITE_URL=http://localhost:3000`** (مسحوب من بيئة development في Vercel). أي بناء إنتاج محلي ينتج روابط localhost في sitemap و metadataBase. لا يضرّ اليوم لأن Vercel يبني بمتغيّراته، لكنه فخ.

---

## G. ترتيب الترحيل المقترح · وأخطر ثلاثة تغييرات

### الترتيب
مراحلك العشر سليمة، وأضيف إليها ثلاثة بنود تدقيق:

| المرحلة | ما يُنجَز | إضافة من التدقيق |
|---|---|---|
| 1 · الرموز | توحيد `@theme` و`:root` في مصدر واحد | **+ حذف أحد عشر رمز `bh-*`** و`app/fonts.ts` |
| 2 · الطباعة و RTL | — | **+ إصلاح العيوب الستة في القسم D** |
| 3 · الترويسة والتذييل | — | **+ حذف المكوّنات الثمانية الميتة** قبل أي عمل، وإلا حُرّر الملف الخطأ |
| 5 · الغلاف | — | ⛔ **محجوبة بتعارض — انظر أدناه** |
| 6 · صفحة الملخص | — | إعادة بناء `summary-full` (222 سطراً) و`summary-free` و`section` — **٩٠٪ من عمل هذه المرحلة** |
| 8 · الأقسام | — | **+ نقل `/c/[slug]` من `SummaryCard` القديم إلى النظام الجديد** |

### أخطر ثلاثة تغييرات

**١ · `app/globals.css` — حذف طبقة `bh-*`.**
أحد عشر رمزاً تستهلكها **سبعة مكوّنات حيّة** عبر عشرات أصناف Tailwind (`bg-bh-surface`، `text-bh-primary-dark`، `border-bh-border`، `divide-bh-border`، `hover:border-bh-primary`…). حذفها قبل إعادة كتابة تلك المكوّنات يجعل الأصناف بلا قيمة فيُعرض النص بلا لون. **الترتيب الآمن: أعد كتابة المكوّن أولاً، ثم احذف رمزه.**

**٢ · `app/s/[slug]/page.tsx` — الجدار المجاني.**
الطبقة الثانية من الحماية هي أن الصفحة **لا تطلب أعمدة `content_full` أصلاً بلا جلسة** (السطور 28-31). أي إعادة هيكلة تمسّ `getSummary` قد تحوّل الطلب إلى `select("*")` فيسرّب المحتوى المدفوع. الطبقة الأولى في Postgres تحمي، لكن الثقة تُكسر أولاً. **اختبار إلزامي: نافذة خاصة على `/s/atomic-habits` + فحص نصّ الصفحة.**

**٣ · الخطوط.**
تغيير الخط يمسّ كل سطر في الموقع، ولا يمكن التراجع عنه جزئياً. وشبكة جهاز التطوير محجوبة، فأي خط جديد يجب أن يُستضاف ذاتياً في `public/fonts` قبل أن يُشار إليه — وإلا علّق البناء بلا رسالة خطأ.

---

## ✅ تعارضان انتهيا · ⛔ ثلاثة قرارات باقية

> وصلت **لوحة الهوية** في 16 سبتمبر. قرأتُها كاملة.

### ✅ انتهى · اللوحة هي المصدر المعتمد
الرموز والخطوط في المواصفة **تطابق اللوحة حرفياً**. الاستبدال مقصود، وأنفّذه بلا اعتراض:

| اللون | HEX | الاستخدام على اللوحة |
|---|---|---|
| أخضر بهجة | `#1D9E75` | تمييز · روابط · CTA |
| أخضر داكن | `#15332A` | عناوين · هيدر · أقسام |
| أخضر فاتح (Mint) | `#3ED69F` | نجاح · إشارات |
| كريمي دافئ | `#F7F5EE` | **الخلفية الرئيسية** |
| أخضر عميق | `#0B1412` | خلفيات داكنة · فوتر |
| أبيض | `#FFFFFF` | **بطاقات · أسطح** |
| رمادي ناعم | `#E6ECE9` | حدود · خلفيات ثانوية |
| ذهبي هادي | `#C99A45` | تمييز · شارات خاصة |

**الخطوط:** Tajawal للعربية · Inter للإنجليزية · H1 48-64 · H2 36-44 · H3 26-32 · Body 16-18 · Caption 12-13 · Button 15-16 Semibold · line-height 1.7-1.9 · letter-spacing 0-0.5px.

> ⚠️ **تنفيذي:** Tajawal (400/500/700/800) و Inter (400/600) **يجب استضافتهما ذاتياً** في `public/fonts` قبل أي إشارة إليهما. شبكة جهاز التطوير محجوبة بـ proxy، و`next/font/google` يعلّق البناء بلا رسالة خطأ. نفس الفخ الذي وقعنا فيه مع Amiri.

### ⛔ القرار الأول · ألوان الأقسام الستة خارج اللوحة

المواصفة تعطي: ريادة الأعمال `#A8432B` · الاستراتيجية `#1F3A5F` · الفرق والثقافة `#6B4A7E`.
**هذه الثلاثة ليست على لوحة الهوية إطلاقاً.** اللوحة كلها أخضر + كريمي + ذهبي. ولا يوجد فيها طوبي ولا كحلي ولا بنفسجي.

وأغلفة الكتب في القسم 7 من اللوحة **كلها خضراء داكنة** — لا نظام ألوان لكل قسم. وخانة «خطوط أغلفة الكتب» على اللوحة **فارغة**: مواصفة طباعة الغلاف لم تُحدَّد أصلاً.

الأثر: الغلاف هو أكثر سطح يُشارَك (لينكدإن وواتساب). ثلاثة ألوان خارج اللوحة تظهر هناك قبل أن تظهر في أي مكان آخر.

**توصيتي:** اشتقّ الأقسام من اللوحة وحدها. أربعة أسطح داكنة متاحة (`#15332A` · `#0B1412` · `#1D9E75` · `#C99A45`)، والتمييز بين الأقسام يحمله **سطر التصنيف وخط النبرة** لا لون كامل. يخسر شيئاً من سرعة المسح البصري على الرفّ، ويكسب اتساقاً كاملاً مع الهوية.
**البديل:** اعتماد الألوان الثلاثة كتوسعة معلنة للّوحة — قرارك، لكن سجّله في اللوحة حتى لا يتناقض المرجع مع الموقع.

### ⛔ القرار الثاني · مواصفة الغلاف ما زالت التصميم المرفوض

المواصفة تعيد وصف الغلاف الحالي حرفياً: خط 28×1 في الأعلى، عنوان، سطر تسمية 10px في الأسفل.
وهذا بالضبط ما وُصف بأنه يحتاج إعادة تصميم: **٦٠٪ فراغ، ولا اسم مؤلف.** واللوحة لم تملأ خانة مواصفة الغلاف.

**توصيتي:** غلاف جديد على اللوحة الجديدة — حقل داكن، عنوان Tajawal 800 بالكريمي، **اسم المؤلف مضافاً**، شريط سفلي أو نقش «نقاط متراكبة» بشفافية منخفضة يملأ الفراغ، وسطر التصنيف في القاع. أي: الاتجاه «أ» منقولاً إلى اللوحة الجديدة.

### ⛔ القرار الثالث · شارات بطاقة الكتاب بلا بيانات

بطاقة الكتاب على اللوحة تحمل: «عملي» · «مختصر» · «القراءة» · «طبقة ١» · «جديد».
**لا وجود لأي من هذه في `bh_summaries`.** الأعمدة المتاحة: `category_id` · `reading_minutes` · `published_at` · `author`.

بحكم القيدين ٣ و٤ (لا اختراع ميزات، لا تغيير نموذج المحتوى):
- ✅ شارة التصنيف — من `category_id`
- ✅ «جديد» — محسوبة من `published_at`
- ❌ «عملي» · «مختصر» · «طبقة ١» — **تُسقَط**، أو يُضاف لها عمود في `bh_summaries` وهذا تغيير في نموذج المحتوى يحتاج قرارك.

### خارج النطاق — مؤكَّد
القسم 8 من اللوحة يعرض أسطحاً غير موجودة في المنتج: **لوحة المستخدم · البريد الأسبوعي · صفحة الاشتراك · منشور الشبكات الاجتماعية**. بحكم القيد ٣ لا تُبنى. المنتج اليوم: رئيسية · أقسام · ملخص · دخول OTP.

## ما لم يتغيّر — تأكيد
لا مكوّن عُدِّل · لا سطر CSS لُمس · لا مسار أُضيف أو حُذف · لا استعلام Supabase مُسّ · لا فرع أُنشئ.
الحالة كما كانت: `6b53d8c` والشجرة نظيفة عدا هذا الملف.
